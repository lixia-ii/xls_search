# xls_search 使用说明

## 启动
双击 `search.bat` 打开图形界面。

## 目录选择
从下拉框选择、点「浏览…」选文件夹，或直接粘贴路径。

## 搜索模式
- **直接读取文件** — 逐文件扫描，慢但结果最新
- **查询索引** — 毫秒级，需先建立索引

## 索引操作（下拉按钮）
- **更新变动索引** — 只重建有变动的文件
- **重建全部索引** — 清空全部重建（首次用）

## 搜索
输入关键字后回车（或点「搜索」），不区分大小写。  
可选：精确匹配 / 文件名过滤 / 限定列号。

## 结果
双击某行复制到剪贴板；点列头排序。
