Metadata-Version: 2.1
Name: rainbow-admin
Version: 0.4.15
Summary: rainbow python admin lib and cli
Home-page: https://git.code.oa.com/rainbow/python-admin
Author: rainbow team
Author-email: kaeljiang@tencent.com
License: UNKNOWN
Platform: any
Classifier: Development Status :: 5 - Production/Stable
Classifier: Programming Language :: Python
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.6
Classifier: Programming Language :: Python :: 3.7
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: Implementation :: CPython
Requires-Python: >=3.6
Requires-Dist: requests (>=2.10.0)
Requires-Dist: click (<8,>=7)
Requires-Dist: tabulate (>=0.8)

python-admin 实现常用admin接口的sdk化，并提供命令行操作工具

# 1. Install
## Requirement
python 版本要求 >=3.6
## 安装命令
```shell script
pip install rainbow-admin -U -i https://mirrors.tencent.com/repository/pypi/tencent_pypi/simple --extra-index-url https://mirrors.tencent.com/pypi/simple/
```

python-admin 支持 cli 命令行，也支持 package 方式 import 使用

# 
# 2. cli 方式的基本用法

## cli 命令行查看帮助
```shell script
# 查看命令行 help
rainbow-admin --help

# 可以在输入子命令后用 -h 来查看子命令的帮助，例如：
rainbow-admin file -h
```

## 指定 app_id 和签名用的用户
```shell script
# 通过命令行传递 app_id、user_id、secret_key
rainbow-admin --app-id d40eecf5-2e89-46e1-9b4a-11ad84c85d83 --user-id 9b8a862de5c74321ac5bc763516fe3be --secret-key d29ff29b635ef58def64d6b0f0bb13d41fbc group ls

# 也可以把 app_id、user_id、secret_key 放环境变量中，这样会话里的 cli 可以省略传递参数
export RAINBOW_APP_ID=d40eecf5-2e89-46e1-9b4a-11ad84c85d83
export RAINBOW_USER_ID=9b8a862de5c74321ac5bc763516fe3be
export RAINBOW_SECRET_KEY=d29ff29b635ef58def64d6b0f0bb13d41fbc
```

## 分组和发布常用命令
```shell script
# 列出 Default 环境的所有group
rainbow-admin group ls

# 指定七彩石环境列出所有 group
rainbow-admin --env formal group ls
```

## kv 型配置常用命令
```shell script
# 列出 GROUP_NAME 的所有 kv
rainbow-admin config -g GROUP_NAME ls

# GROUP_NAME 中新增或修改 KEY_NAME
rainbow-admin config -g GROUP_NAME set KEY_NAME "KEY_VALUE"

# GROUP_NAME 中删除 KEY_NAME
rainbow-admin config -g GROUP_NAME delete KEY_NAME
```

## 文件型配置常用命令
```shell script
# 列出分组所有文件
rainbow-admin file -g GROUP_NAME ls

# 上传文件。LOCAL_FILE_NAME 是本地文件名，FILE_NAME 是上传后的文件名（默认保持与 LOCAL_FILE_NAME 相同即可）
rainbow-admin file -g GROUP_NAME upload FILE_NAME LOCAL_FILE_NAME

# 上传文件，同时解开打包文件，支持 tar、tgz、zip 格式的打包文件。注意这里的本地文件与上传后的文件保持相同
rainbow-admin file -g GROUP_NAME upload -z ZIP_FILE ZIP_FILE

# 删除文件 FILE_NAME
rainbow-admin file -g GROUP_NAME delete FILE_NAME

# 下载单个文件 FILE_NAME
rainbow-admin file -g GROUP_NAME download FILE_NAME

# 下载分组所有文件
rainbow-admin file -g GROUP_NAME download-all
```

## table 型配置常用命令
```shell script
# 列出分组所有行
rainbow-admin table -g GROUP_NAME ls

# 添加一行
# add 命令不带参数可查看 add 的用法: rainbow-admin table -g GROUP_NAME add
rainbow-admin table -g GROUP_NAME add COL_1 COL_2 COL_3

# 修改自增 ID 为 _AUTO_ID 的行
# update 命令不带参数可查看 update 的用法: rainbow-admin table -g GROUP_NAME update
rainbow-admin table -g GROUP_NAME update -n _AUTO_ID COL_1 COL_2 COL_3

# 删除自增 ID 为 _AUTO_ID 的行
rainbow-admin table -g GROUP_NAME delete _AUTO_ID
```


# 3. SDK方式的基本用法
## 基本用法
```python
# 初始化 admin 对象
from rainbow_admin import RainbowAdmin
admin = RainbowAdmin(app_id="d40eecf5-2e89-46e1-9b4a-11ad84c85d83", user_id="9b8a862de5c74321ac5bc763516fe3be", secret_key="d29ff29b635ef58def64d6b0f0bb13d41fbc", timeout=5)

# 列出所有group
print(admin.groups)

# 创建group
admin.create_group("api.new_group")

# 获取group实例
group = admin["api.new_group"]

# 打印所有配置
print(group.configs)

# 设置kv配置
group["key1"] = "value"
group["key2"] = {"foo": "bar"}

# 另一种设置配置的方法：使用kv实例设置配置
kv = group.get_config("key")
kv.value = "value changed"
kv.save()

# 切换环境。注意这里重新赋值了 prod_admin
prod_admin = admin.switch_env("prod")
print(prod_admin.groups)
```

## 文件型分组操作
```python
# 创建文件分组
admin.create_group("file_group", config_type="file")

# 文件型group相关操作
group = admin["api.file_test"]

# 文件型分组里的文件都是存在cos里的，如果所在环境没有外网，或者想从内网下载cos文件，设置 group.is_intranet = True
# 注意此选项必须在调用get_file前设置
group.is_intranet = True

# 上传/更新文件
with open(FILE, "rb") as f:
    group.upload_file("filename", f, "上传测试")  # filename 是 f 上传后重命名的文件名
group['filename'] = "hello world!"  # 语法糖，创建名为 filename 的文件，内容为 "hello world!"

# 获取文件
file = group.get_file("filename")
# 设置内网选项，从北极星解析cos ip并下载文件，避免从cos的公网地址下载
# group.is_intranet = True

print(file.url)  # 文件的下载链接
file.download_to(".")  # 下载到指定目录
file.download_to("./new_filename")  # 下载到指定文件
print(file.text())
print(file.content())

# 删除文件
file.delete()   # 调用file实例的delete()方法
group.delete_file("filename")  # 或者调用分组的delete_file方法
```

## table 型分组操作

```python
admin.create_group("table_test", config_type="TABLE")
group = admin["table_test"]

# 获取指定类型列的默认属性
text_column = ColumnType.default_column(ColumnValueType.TEXT, "text", "text")
text_column.required = False
# 创建表
group.create_table(
    "table",  # 表名
    "description",  # 表描述
    # 表包含哪些列
    [
        ColumnType.default_column(ColumnValueType.AUTO_ID, "_auto_id", "id"),  # 第一列必须是自增id
        ColumnType.default_column(ColumnValueType.NUM, "num", "num"),
        text_column,
    ],
)
group = self.admin["table_test"]

# 增加一列
data = {"num": "123"}
row_id = group.add(data)
print(data["_auto_id"])  # _auto_id will be set
assert data["_auto_id"] == str(row_id)

# 查询表数据
for i in group.data:
    print(i)

# 更新
data["num"] = "345"  # value must be str type
group.update(row_id, data)

# 删除一行
group.delete(row_id)
print(group.data)
```

# 4. 发布

## 一键发布
### one_click_release 一键发布
参数说明：
- creator: 发布人，请使用自定义用户名，即 user_id 对应的用户名, Rainbow_ 开头的名称
- updators: 校验配置修改人。在通过 admin 工具修改配置时，修改人即自定义用户名, 所以 updators 也使用自定义用户名
- approvers: 审批人，请使用自定义用户名。请确保它有管理权限，可以审批发布任务

sdk 方式
```python
# GROUP_NAME 即分组名, USER 即自定义用户名(Rainbow_开头的名称)
group = admin[GROUP_NAME]
group.one_click_release(creator=USER, updators=USER, approvers=USER)
```

cli 方式
```shell script
# GROUP_NAME 即分组名, USER 即自定义用户名(Rainbow_开头的名称)
rainbow-admin group one-click-release  GROUP_NAME --creator USER --updators USER --approvers USER
```


### easy_release 一键发布(已废弃)
~~自动创建任务、全量发布、关闭任务（要求开启分组的免审批功能）
通过设置`force_ignore_approval=True` 可以自动开启分组的免审批的功能。~~

`group.easy_release()` 已废弃.
请使用 `group.one_click_release()`: 版本的发布、全量、任务关闭 三个操作只需要一次请求调用，避免中间状态以及超时问题。

sdk 方式
```python
group = admin[GROUP_NAME]
group.easy_release()
```

cli 方式
```shell script
rainbow-admin group easy-release GROUP_NAME
```

## 新版（>=0.4.0）灰度发布
新版灰度发布支持多步骤灰度功能

**sdk方式**
```python
# import release 相关 class
from rainbow_admin.release import *


# 创建发布任务
task_id = group.create_release_task(
    creator="Rainbow_fa", # 自定义用户名
    version_name='v_123444', # 要保存的版本名
    description='by api', # desc
    updators='someone;Rainbow_fa', # 检查配置的修改人
    approvers='someone;Rainbow_fa', # 指定审批人
    type=0 # 0-或签，1-会签
)
# 开启多版本发布时，创建发布任务
task_id = group.create_multi_version_release_task(
    creator='Rainbow_fa', # 自定义用户名
    version_name='v_123444', # 要发布的已保存的版本名
    target_tags=json.dumps({"region":"tj"}), # 指定目标实例的tag
    desc='from api', # desc
    approvers='somone;Rainbow_fa', # 指定审批人
    type=0,# 0-或签，1-会签
)
# 获取发布任务
task = admin.get_release_task(task_id)
# 获取任务状态，返回数据中的 task_info 是发布任务状态信息，其中 status 是任务的整体状态
task_info = task.get_info()

# 审批通过任务
task.agree()
# 审批拒绝任务
task.reject("填写拒绝理由")

# 初始化一个灰度步骤实例，参数是步骤名
step = GrayStep("s1")
# 灰度步骤添加 tag 规则，格式：tag operator values。可以用 .add_tag 串联多个
# 当前支持 TagOperator.IN（列表判断）、TagOperator.MATCH（正则匹配） 这两种规则
step.add_tag("ip", TagOperator.IN, "10.0.0.1,10.0.0.2").add_tag("dockername", TagOperator.MATCH, "ins[0-9]")
# 也可以直接带 tag 规则来初始化一个步骤
step = GrayStep("s1", "ip", TagOperator.IN, "10.0.0.10")
# 发布任务的多灰度步骤，参数是灰度实例的list
steps = [step]
# 再增加一个灰度步骤实例
steps.append(GrayStep("s2", "uin", TagOperator.MATCH, "[0-9]1234"))
# 覆盖式提交多个灰度步骤
task.change_gray_steps(steps)


# 发布下一个步骤并返回这个步骤本身，没有步骤可发布时返回 None
step = task.gray_next()
# 回滚最后的步骤并返回这个步骤本身，没有步骤可回滚时返回 None
step = task.rollback_last()
# 获取所有灰度步骤及发布状态
steps = task.get_gray_steps()
# 通过修改每个灰度步骤的发布状态，来批量提交灰度发布
# 注意：服务端限制了，发布时要从前往后依次发布，回滚时要从后往前依次回滚
# 使用 .ON() 来标记步骤为已发布，使用 .OFF()来 标记步骤已回滚
steps[0].ON()
steps[1].ON()
steps[2].OFF()
# 通过批量提交步骤状态来批量灰度
task.gray_batch(steps)


# 灰度/全量状态时，回滚到待发布状态
task.rollback()
# 待发布/灰度状态时，全量发布
task.all_in()

# 完成发布任务，待发布、已全量状态时都可以完成任务
task.close_task()
```

**cli方式**

*待补充*

## 旧版（<0.4.0）灰度发布
sdk 方式
```python
# 创建发布任务
task_id = group.create_release_task(USER_NAME, version_name='v_123444')
# 参数参考：create_release_task(self, creator, version_name=None, description='', updators='')
# 获取创建的发布任务实例
task = admin.get_release_task(task_id)
# 获取当前任务的灰度规则，如果没有设置，返回None
rule = task.gray_rule()
# 获取当前项目支持的所有灰度类型
types = admin.rule_types
# 筛选出想要的灰度类型，比如 podname类型
ip_gray_type = [i for i in types if i.type_name == 'podname类型'][0]
# 给当前任务设置灰度规则
task.set_gray_rule(ip_gray_type, "ins-abcde\nins-123463") # 多个规则内容用 \n 分隔
# 当前任务灰度生效
task.gray()
# 当前任务全量发布
task.all_in()
# 当前任务回滚
task.rollback()
```

cli 方式
```shell script
# 查看所有灰度类型，会在后面使用
rainbow-admin release rule-types

# 获取指定task_id的当前灰度规则，如果还未设置，报错且非0返回
rainbow-admin release get-gray-rule 2782843

# 设置指定task_id的灰度规则，参数顺序为 task_id, rule_type, rule_content
# 其中rule_type 可以是id，也可以是类型名
rainbow-admin  release set-gray-rule 2782843 2 '1.1.1.1'
或
rainbow-admin  release set-gray-rule 2782843 ip类型 '1.1.1.1'
```

# 5. API 文档
python-admin 中有完善的 docstring 帮助理解 api 的用法，可以通过 python 的 help 来查看

用法举例:

**直接 import rainbow_admin，查看 help**
```
# python3
Python 3.8.13 (default, Mar 16 2022, 20:33:22)
[Clang 13.0.0 (clang-1300.0.29.30)] on darwin
Type "help", "copyright", "credits" or "license" for more information.
>>> import rainbow_admin
>>> help(rainbow_admin)
```
**查看子类的 help**
```
>>> help(rainbow_admin.group)
>>> help(rainbow_admin.file)
>>> help(rainbow_admin.table)
>>> help(rainbow_admin.release)
```


# 6. 在 feflow 使用 python-admin
rainbow-admin 已上架 [feflow商店](http://feflow.oa.com/encology/)
```shell script
fef install feflow-plugin-rainbow-admin
```

# 7. Contributing
提交代码前请使用 `flake8` 静态检查代码，使用 `black` 格式化代码。保持仓库清真。

# 8. TODO
- [x] 私有表与公共表关联的配置与测试
- [x] TABLE型group
- [x] 版本发布逻辑
- [ ] API doc

# 9. ChangeLog
### v0.2.4
- 支持获取、设置灰度规则
- 修复文件cli格式化时，0大小文件报错问题

### v0.2.3
- table分组add返回row_id
- kv 增加 operator 字段，file 增加 operator、file_version 字段、修复 mtime 在 cli 下没对齐
- kv分组增加 YAML 数据类型

### v0.2.2
- 支持获取文件型文组的文件下发信息
- table型分组创建表时支持设置required

### v0.2.1
- fix #6: 支持内网下载cos文件

### v0.2.0
- fix #3: 新增group.one_click_release() 替代 group.easy_release()，版本的发布、全量、任务关闭 三个操作只需要一次请求调用，避免中间状态以及超时问题。

### v0.1.21
- 创建发布任务时，支持指定修改人. 指定修改人时，会检查修改人，相当于页面上的 “只能有我的改动”

### v0.1.20
- 支持加密的文件型分组下载

### v0.1.19
- fix #3: 当release.all_in()超时，为避免卡下一次发布，release.close()放在finally里
- fix #4: get_release_task() 增加group_name参数，调用时增加env_name参数，Group增加get_release_task()方法.

### v0.1.18
- cli: 创建文件型分组时，可选关闭存储加密

## v0.1.17
- rainbow cli 支持timeout参数

## v0.1.16
- 创建文件型分组时，可选关闭存储加密

## v0.1.15
- 修复公共分组同步功能

## v0.1.14
- 修复创建table后无法调用table方法的问题

## v0.1.13
- table型分组支持api创建表

## v0.1.6
 - kv型group，读取写入值时类型自动转换：
 - 读取时，如果value_type 是number,json,date 类型，自动转换为python类型
 - 写入时，如果value_type没有指定，自动判断值类型并设置value_type

## v0.1.5
- 新增切换环境功能
- sdk，admin删除`add_group`方法，请使用`create_group`
- cli，删除`admin group add`, `admin group configs`

## v0.1.2
- 修复create_group的bug
- RainbowAdmin 构造函数`api`参数移至最后

## v0.1.0
- 支持file型分组
- 对外import 删除`Group`, `KeyValue`

# 9. Reference
- [方案设计](https://iwiki.oa.tencent.com/pages/viewpage.action?pageId=111994414)
- [Admin SDK开发方案](https://iwiki.oa.tencent.com/pages/viewpage.action?pageId=111994414)
- [API认证算法说明](https://iwiki.oa.tencent.com/pages/viewpage.action?pageId=74063081)
- [【常用】Admin接口的说明](https://iwiki.oa.tencent.com/pages/viewpage.action?pageId=98145038)
- [【全】Admin接口（proto文件生成）](https://iwiki.oa.tencent.com/pages/viewpage.action?pageId=80188100)


